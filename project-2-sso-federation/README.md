# Project 2: SSO & Federated Authentication Lab (SAML 2.0 / OIDC)

**Status:** Not started — scheduled for Week 2

## Objective
Demonstrate how modern cloud authentication protocols securely pass identity assertions
between an Identity Provider (IdP) and a Service Provider (SP).

## Planned Deliverables
- [ ] SAML 2.0 integration walkthrough (Entra ID/Okta → target app)
- [ ] `samples/` — annotated JWT breakdown (ID Token, Access Token, Refresh Token claims)
- [ ] Conditional Access (MFA) policy documentation with enforcement screenshots
- [ ] `docs/` — network trace screenshots, architecture diagram
